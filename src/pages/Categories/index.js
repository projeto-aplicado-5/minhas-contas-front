import React from 'react'

import Template from '../../components/Template' 

import api from '../../services/api'

import './categories.css'

export default function Categories() {


    return (
        <Template>
            <h1>Oi, eu sou o conteudo de categorias.</h1>
        </Template>
    );
}